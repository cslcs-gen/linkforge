# LinkForge

A standalone browser puzzle where players group hidden connections across 16 tiles.

## Run locally

Open `index.html` in a browser.

## Notes

- Gameplay runs entirely in the browser.
- Visitor count uses CounterAPI V1 with namespace `linkforge` and counter `visits`.
- The same browser increments at most once every 6 hours.
